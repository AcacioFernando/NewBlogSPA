<div class="top-header">
    <div class="container">
        <nav class="navbar navbar-default">
            <div class="container-fluid">
                <!-- Brand and toggle get grouped for better mobile display -->
                <div class="navbar-header">
                    <button type="button" class="navbar-toggle collapsed" data-toggle="collapse"
                            data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
                        <span class="sr-only">Toggle navigation</span>
                        <span class="icon-bar"></span>
                        <span class="icon-bar"></span>
                        <span class="icon-bar"></span>
                    </button>
                    <div class="logo">
                        <a href="/"> <img src="/images/logo.png" alt=""/></a>
                    </div>
                </div>

                <!-- Collect the nav links, forms, and other content for toggling -->
                <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
                    <ul class="nav navbar-nav">
                        <li><a href="/">Home<span class="sr-only">(current)</span></a></li>
                        <li class="dropdown">
                            <a class="dropdown-toggle"
                               data-toggle="dropdown"
                               href="#">
                                Categoria
                                <b class="caret"></b>
                            </a>
                            <ul class="dropdown-menu">
                                <% if(categorias != null){ %>
                                <% categorias.forEach(function(categoria){ %>

                                    <li><a href='/categoria/<%= categoria.nome_categoria%>'><%= categoria.nome_categoria%></a></li>
                                <% })} %>

                            </ul>
                        </li>


                    </ul>
                    <div class="search">
                        <form method="post" action="/search">
                            <input name="search_noticia" id="search_noticia" type="search" value="Search"
                                   onfocus="this.value = '';" onblur="if (this.value == '') {this.value = 'Search';}"
                                   required="">
                            <button type="submit" class="btn btn-default" aria-label="Left Align">
                                <span class="glyphicon glyphicon-search" aria-hidden="true"></span>
                            </button>

                        </form>
                    </div>
                    <% if(usuario == null || usuario == []){ %>
                    <ul class="nav navbar-nav navbar-right">
                        <button type="button" class="btn btn-admin btn-lg" data-toggle="modal" data-target="#myModal"><i
                                    class="fa fa-gears"></i></button>
                    </ul>
                    <% }else{ %>
                    <ul class="nav navbar-nav navbar-right">
                        <button type="button" class="btn btn-admin btn-lg" onclick="location.href = '/admin';"><i
                                    class="fa fa-gears"></i></button>
                    </ul>
                    <% } %>
                </div><!-- /.navbar-collapse -->
            </div><!-- /.container-fluid -->
        </nav>
    </div>
</div>

</head>

<!-- Modal -->
<div class="modal fade" id="myModal" role="dialog">
    <div class="modal-dialog">

        <!-- Modal content-->
        <div class="modal-content">
            <div class="modal-header">
                <h4><span class="glyphicon glyphicon-lock"></span> Login</h4>
            </div>
            <div class="modal-body">

                <form method="post" action="/admin/login">
                    <div class="form-group">
                        <label for="usrname"><span class="glyphicon glyphicon-user"></span> Username</label>
                        <input type="text" class="form-control" name="admin_username" id="admin_username"
                               placeholder="Enter email">
                    </div>
                    <div class="form-group">
                        <label for="psw"><span class="glyphicon glyphicon-eye-open"></span> Password</label>
                        <input id="admin_password" class="form-control" name="admin_password" type="password"
                               class="form-control" placeholder="Entre com a senha">
                    </div>

                    <div class="row btn-login">
                        <div class="col-md-6">
                            <button type="submit" class="btn btn-success btn-block"><span
                                        class="glyphicon glyphicon-off"></span> Login
                            </button>
                        </div>
                        <div class="col-md-6">
                            <button type="button" data-dismiss="modal" class="btn btn-success btn-block"><span
                                        class="glyphicon glyphicon-remove"></span> Cancelar
                            </button>
                        </div>
                    </div>
                </form>
            </div>
            <div class="modal-footer">
                <p>Não é membro? <a href="/admin/registro">Registrar</a></p>
            </div>
        </div>

    </div>
</div>
</div>






